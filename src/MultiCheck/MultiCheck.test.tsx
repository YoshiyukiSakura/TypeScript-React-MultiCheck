// TODO more tests

describe('MultiCheck', () => {
  describe('initialize', () => {
    it('renders the label if label provided', () => {
      // sorry Im new to here, will learn how to write tests later
    });
  });
});
